[package]
name = "kobod"
version.workspace = true
edition.workspace = true
publish = false
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "kobod"
path = "src/main.rs"

[dependencies]
kobo-abi = { path = "../kobo-abi" }
kobo-app-store = { path = "../kobo-app-store" }
kobo-hal = { path = "../kobo-hal", default-features = false }
kobo-net = { path = "../kobo-net" }
kobo-policy = { path = "../kobo-policy" }
kobo-shell = { path = "../kobo-shell" }
kobo-profile = { path = "../kobo-profile" }
kobo-protocol = { path = "../kobo-protocol" }
kobo-text = { path = "../kobo-text" }
kobo-ui = { path = "../kobo-ui" }

[dev-dependencies]
# The runtime tells a keyboard apart from any other grid of cells by its shape.
# Testing that against a hand-built imitation only proves the imitation, so the
# tests build the real composite the SDK draws.
kobo-sdk = { path = "../kobo-sdk" }

[lints]
workspace = true

[features]
default = []
device-write = ["kobo-hal/device-write"]
