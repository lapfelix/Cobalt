# Fixture-backed queue states. The proxy fixture should expose one
# auth_required job and one hook_failed job in the saved job list.
clean
scenario normal
tap Queue
wait-for Server queue
expect Authentication needed
expect Send failed
